Warum der Fehler bei dir so hartnäckig ist

Error [TokenInvalid] kommt nur aus drei echten Gründen:

1. Der Token kommt im Code leer oder verändert an

Das ist der häufigste Fehler bei Replit + Render.

Baue DAS einmal ein, nur zum Testen:

console.log("TOKEN LENGTH:", process.env.DISCORD_TOKEN?.length);


Erwartung:

eine Zahl zwischen 59–72

wenn undefined, 0 oder NaN → Environment kaputt

Wenn da nichts Sinnvolles kommt, ist nicht Discord das Problem, sondern:

falscher Variablenname

Render hat alten Cache

falscher Service (Web vs Worker)

2. Dein Bot läuft als Web Service, nicht als Worker

Render ist da extrem streng.

Wenn du einen Discord-Bot hast, dann:

❌ NICHT Web Service
✅ Background Worker

Warum?

Web Services erwarten HTTP

Discord-Bots brauchen dauerhafte WebSocket-Verbindung

Render killt sonst intern den Prozess

👉 In Render:

New → Background Worker

Repo auswählen

kein Port

Start Command:

node dist/index.cjs


Das ist kein Nice-to-have, das ist Pflicht.

3. Du hast mehrere Bots / Apps durcheinander

Auch extrem häufig.

Bitte prüfen:

Genau die App, deren Token du nutzt

Genau dieser Bot ist auch auf deinem Server eingeladen

nicht alte App, nicht Test-Bot, nicht zweite Application

Tipp:

Bot-Name ändern

Avatar ändern

dann siehst du sofort, welcher online gehen müsste

Sachen, die du im Developer Portal AUS machen solltest

Im Bot-Tab:

❌ Requires OAuth2 Code Grant → AUS

❌ Interactions Endpoint URL → leer

❌ Public Bot → egal, aber fürs Testen ruhig AUS

Im OAuth2-Tab:

Redirects → leer

nichts konfigurieren

Dein Bot braucht kein OAuth-Flow, Punkt.

Minimal-Code zum Gegencheck (bitte exakt so)
import { Client, GatewayIntentBits } from "discord.js";

const client = new Client({
  intents: [
    GatewayIntentBits.Guilds,
    GatewayIntentBits.GuildMessages,
    GatewayIntentBits.MessageContent
  ],
});

client.once("ready", () => {
  console.log("LOGGED IN AS", client.user?.tag);
});

client.login(process.env.DISCORD_TOKEN);


Wenn DAS mit frischem Token + Worker nicht online geht, dann:

kommt der Token nicht korrekt an

oder Render nutzt noch einen alten Build

Ganz wichtiger Render-Reset-Move

Bitte genau so:

Render → Service löschen

NEUEN Background Worker erstellen

Environment Variables neu eintippen, nicht kopieren

Deploy → Clear Cache & Deploy

Render cached gerne kaputte ENV-States. Löschen hilft öfter als man denkt.

Noch ein ehrliches Wort

Du hast nichts „falsch programmiert“.
Das ist ein klassischer Mix aus:

Token-Handling

falschem Service-Typ

Render-Eigenheiten

Wenn du willst, sag mir:

ob du jetzt Worker oder Web Service nutzt

was TOKEN LENGTH ausgibt