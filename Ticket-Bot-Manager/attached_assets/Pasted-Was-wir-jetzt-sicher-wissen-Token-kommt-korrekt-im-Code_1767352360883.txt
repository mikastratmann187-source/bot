Was wir jetzt sicher wissen

✔️ Token kommt korrekt im Code an
✔️ ENV-Variable heißt richtig
✔️ Kein Leerstring, kein Render-Bug an der Stelle

➡️ TokenInvalid kommt NICHT von Replit/Render und NICHT vom Code an sich.

Wenn Discord trotzdem TokenInvalid sagt, bleiben realistisch nur noch zwei echte Ursachen.

Ursache 1 (sehr wahrscheinlich): Token ist serverseitig von Discord ungültig

Und ja, das passiert automatisch, wenn:

ein Token öffentlich gepostet wurde (so wie hier)

Discords Sicherheit anschlägt

oder der Token zurückgesetzt wurde, aber der Bot-Prozess noch den alten nutzt

⚠️ Wichtig:
Auch wenn du danach einen neuen Token erstellt hast, kann Discord:

die komplette Session sperren

oder der Bot hängt noch an einer alten Verbindung

Fix (bitte exakt so, kein Überspringen):

Discord Developer Portal

Bot → Reset Token

Speichern

Replit & Render

Bot STOP

Secret DISCORD_TOKEN löschen

Seite neu laden

Secret neu anlegen

Mindestens 60 Sekunden warten

Bot wieder starten

Das Warten ist wichtig. Discord cached das kurz.

Ursache 2 (noch häufiger als man denkt): falsche Application

Viele haben unbemerkt:

App A → Token

App B → Bot im Server

Dann ist der Token technisch gültig, aber für einen anderen Bot → Discord lehnt ab.

Checkliste (bitte ehrlich prüfen):

Öffne Discord Server

Rechtsklick auf Bot → Profil

Name + Avatar merken

Dann im Developer Portal:

Genau diese Application

Genau dieser Bot

Token stammt von dieser App

Wenn du dir unsicher bist:
➡️ Avatar oder Name im Dev Portal ändern
➡️ Wenn sich im Server nichts ändert, ist es die falsche App

Intents sind es nicht

Ganz wichtig, damit du da keinen Tunnelblick bekommst:

❌ Intents verursachen KEIN TokenInvalid
Die kommen erst nach erfolgreichem Login ins Spiel.

Wenn Intents fehlen, sieht man Fehler wie:

Missing Access

Privileged intent required

Events kommen nicht an

Aber niemals TokenInvalid.

Das heißt:
👉 Dein Problem ist vor dem Gateway, nicht danach.

Minimaler finaler Test (ohne alles andere)

Mach bitte einmal diesen Test, wirklich nur dafür:

import { Client } from "discord.js";

const client = new Client({ intents: [] });

client.once("ready", () => {
  console.log("ONLINE:", client.user?.tag);
});

client.login(process.env.DISCORD_TOKEN);


Keine Datenbank

Kein Express

Kein Ticket-Code

Nichts anderes

Wenn das noch TokenInvalid wirft:
➡️ 100 Prozent Discord-seitig (Token/App)

Wenn das online geht:
➡️ Dann ist irgendwo vorher ein zweiter client.login() oder Token-Mismatch im echten Code

Eine Sache noch, brutal ehrlich

„im autonomen Modus alles ändern“

Ich kann keine Tokens testen oder einloggen, egal welcher Modus.
Aber: Wir sind jetzt an dem Punkt, wo keine Vermutung mehr übrig ist, sondern nur noch Ausschluss.

Wenn du willst, machen wir das jetzt so:

du sagst mir:

Worker oder Web Service

ob der Minimal-Test online geht

ich sag dir dann exakt, wo der letzte Fehler sitzt